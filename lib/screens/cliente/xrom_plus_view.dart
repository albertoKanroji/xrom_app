import 'package:flutter/material.dart';

class XromPlusView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Xrom+'),
      ),
      body: Center(
        child: Text(
          'Vista de Xrom+',
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}
